<ul>
  <% @jobs.each do |job| %>
  <li>
    <%= link_to job.title, job_path(job) %> - <%= link_to job.company.name, company_path(job.company) %>, <%= job.company.location %>
  </li>
  <%end%>
</ul>

<p><%= link_to "Company List", companies_path %></p>

<p><%= link_to "New Company", new_company_path %></p>
<p><%= link_to "New Job", new_job_path %></p>
